# ai-tools-api

A private HTTP API for small, reusable AI-backed tools. Release 0.1 provides
bounded speech transcription and synthesis.

## Quickstart

```sh
nix run .
# See docs/runtime.md for local requests and deployment configuration.
```
